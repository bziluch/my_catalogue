<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand navbar-title" href="#">My<span class="title-pt2">Catalogue</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_default') }}">Strona główna</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_default') }}">Katalogi</a>
                </li>
            </ul>
            <div class="d-flex">
                {% if app.user is not null %}
                    <div>
                        <a href="{{ path('app_logout') }}">
                            <button type="button" class="btn btn-outline-warning"><i class="fa-regular fa-user"></i> Wyloguj</button>
                        </a>
                    </div>
                {% else %}
                    <div>
                        <a href="{{ path('app_login') }}">
                            <button type="button" class="btn btn-outline-info"><i class="fa-regular fa-user"></i> Zaloguj</button>
                        </a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>